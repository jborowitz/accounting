services:
  accounting-demo:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accounting-demo
    restart: unless-stopped
    ports:
      - "8002:8002"
    volumes:
      - ./data:/app/data:ro
